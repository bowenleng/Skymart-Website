# todo: store an AI agent's token in the front end

TOKEN = ""

def talk_to_agent(message):
    # This function would send a message to the AI agent and return its response.
    # For now, it just returns a placeholder response.
    return "placeholder text"